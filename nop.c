#include "monty.h"
/**
 * nop - does nothing
 *
 * @stack: the stack
 *
 * @line_number: number of the line were opcode is
*/
void nop(stack_t **stack, unsigned int line_number)
{
	(void)stack;
	(void)line_number;
}
